import '../styles/main.scss';
import quadrant from './components/quadrant';

const init = () => {
  quadrant.fightQuad();
  quadrant.sleepQuad();
  quadrant.eatQuad();
  quadrant.playQuad();
};

init();
